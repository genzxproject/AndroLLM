<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility automation engine configuration. The service reads window
    content, performs gestures and takes screenshots. It listens for window
    state / content changes, scrolls and focus moves so the UI state tracker
    stays fresh without polling (keeps CPU usage low).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged|typeViewScrolled|typeViewFocused"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="50"
    android:label="@string/accessibility_service_label" />
